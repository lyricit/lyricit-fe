export type TrackProps = {
  id: string;
  imageUrl: string;
  title: string;
  artist: string;
};

export type TrackDetailProps = {
  id: string;
  title: string;
  artist: string;
  imageUrl: string;
  lyrics: string;
};
